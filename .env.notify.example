# Copy to repo-root .env.notify (gitignored).

NOTIFY_TO=you@gmail.com

# --- Recommended on this host (SMTP is blocked) ---
# 1) https://script.google.com → New project → paste Apps Script below → Deploy → Web app
#    Execute as: Me | Who has access: Anyone (or Anyone with Google account)
# 2) Paste the /exec URL here:
NOTIFY_WEBHOOK_URL=

# Apps Script:
# function doPost(e) {
#   var d = JSON.parse(e.postData.contents);
#   GmailApp.sendEmail(d.to, d.subject, d.body || '');
#   return ContentService.createTextOutput('ok');
# }

# --- Optional HTTPS alternative ---
# RESEND_API_KEY=   # or put in .env.resend
# NOTIFY_FROM=Potions <onboarding@resend.dev>

# --- Cursor headless CLI ---
# Prefer repo-root .env.cursor (gitignored):
#   CURSOR_API_KEY=crsr_...
# Install CLI: curl https://cursor.com/install -fsS | bash


# Manual:
#   scripts/run_completion_report_agent.sh live/state/fx_index_metals_st_pmc_runner_variants --both
#   scripts/arm_completion_watchers.sh

# --- Gmail SMTP (will NOT work here: ports 465/587 timed out) ---
# GMAIL_USER=you@gmail.com
# GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx
